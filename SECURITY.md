# 安全策略 (Security Policy)

感谢你关注 CF-KVR2-NetworkCloud 的安全性。本文件说明如何报告安全漏洞、项目的安全实践，以及我们的响应承诺。

## 🚫 请勿公开披露

如果你发现了一个安全漏洞，**请不要**通过公开 Issue、Pull Request、社交媒体或任何公开渠道披露。公开披露会把所有用户（包括尚未更新的人）置于风险之中。

请通过下面的私密渠道报告，给我们留出修复时间。

## 📧 如何报告漏洞

请通过以下任一方式私密报告：

- **Email**：[asiacomk@gmail.com](mailto:asiacomk@gmail.com)（主题建议标注 `[SECURITY]`）
- **GitHub 私密报告**：仓库 → Security 标签页 → **Report a vulnerability**（推荐，走 GitHub 私密 Issue 机制）

报告时请尽量包含：

1. 漏洞的类型和位置（哪个功能、哪个 API 端点、哪行代码）
2. 复现步骤（越详细越好，最好有 PoC 或截图）
3. 潜在影响（能造成什么后果）
4. 建议的修复方式（可选，但很欢迎）

### 一份好的报告示例

```
标题：[SECURITY] /api/share 存在越权访问

位置：handleShareData() 函数

复现：
1. 用户 A 创建带密码的分享链接
2. 直接访问 /s/<token>/dl 端点，不带密码参数
3. 文件被直接下载，绕过了密码校验

影响：分享链接的密码保护可被绕过

建议：在 /dl 和 /pv 端点同样校验 share.password
```

## ⏱️ 响应时间线

| 阶段 | 承诺时间 |
|------|----------|
| 确认收到 | 3 个工作日内 |
| 评估与复现 | 7 个工作日内 |
| 修复并发布 | 视严重程度，通常 7–14 天 |

严重漏洞（如可导致数据泄露、越权、远程代码执行）会优先处理。修复后我们会告知你进展，并在你同意后于安全公告（Security Advisory）中致谢。

## 🛡️ 已采取的安全措施

本项目在设计和实现中已内置以下防护：

### 认证与会话

- 密码登录 + 7 天有效期 Session Token
- Token 通过 URL 参数传递，客户端存于 `sessionStorage`（非 `localStorage`，不落 Cookie），规避 CSRF 与 XSS 窃取面
- 支持独立的 WebDAV / API 访问令牌，可设只读权限，可随时撤销

### 输入校验与转义

- **XSS 防护**：所有用户输入（文件名、路径、备注、标签）在拼入 DOM 前一律 `esc()` / `escHtml()` 转义
- **路径穿越防护**：`normPath()` 解析并归一化 `..`，限制访问在根目录内
- **文件名过滤**：`sanitizeName()` 过滤 `\ / : * ? " < > |` 及控制字符
- **服务端零信任**：所有 `/api/*`（登录、分享、上传链接除外）强制 token 校验

### 数据保护

- **文件夹加密**：独立密码（SHA-256 hash 存储），访问需解锁
- **客户端加密**：AES-256-GCM + PBKDF2（10 万次迭代）派生密钥，**服务端零知识**——密文 `.enc` 即使 R2 数据泄露也无法解密
- **分享链接**：可设访问次数上限 + 过期时间 + 密码

### 运行时

- 运行于 Cloudflare Workers V8 隔离沙箱，无持久文件系统、无原生模块
- 所有 KV / R2 读取包裹 try/catch，异常不外泄堆栈

## ⚠️ 责任边界（重要）

这是一个供**个人自托管**的开源项目。以下情形属于部署者的责任，而非代码本身的漏洞：

| 情形 | 说明 |
|------|------|
| 弱密码 | `DRIVE_PASSWORD` 设成 `123456` 这类弱口令 |
| 令牌泄露 | 把访问 Token 或加密密码泄露给他人 |
| 配置错误 | 未设密码、把敏感文件夹设为公开分享 |
| 上游依赖 | Cloudflare 平台本身、或加载的外部 CDN 库的问题 |
| 数据丢失 | 未做备份导致 R2/KV 数据丢失 |

> 客户端加密的密码一旦丢失，**任何人都无法恢复**（包括作者）。这是零知识设计的必然结果，不是漏洞。

## 🔒 安全版本支持

| 版本 | 是否支持安全更新 |
|------|------------------|
| v5.x（当前） | ✅ |
| v4.x | ⚠️ 仅关键漏洞 |
| ≤ v3.x | ❌ 不再支持 |

请始终部署最新版本。安全修复只随新版本发布，不单独提供旧版补丁。

## 📢 安全公告

重大安全修复会同时通过以下方式通知：

- GitHub **Security Advisories**（仓库 Security 标签页）
- 更新日志（见 Wiki / CHANGELOG）

建议 **Watch → Releases** 本仓库以接收通知。

## 🙏 致谢

我们真诚感谢每一位负责任地报告漏洞的研究者。经你同意后，我们会在安全公告中署名致谢，并在 README 的贡献者区表达感谢。

## 适用范围

本策略适用于本仓库的源代码。部署实例（如 `cloud.bjhr.space`）的安全问题同样欢迎报告，但请注意：**不要对未授权的第三方部署进行扫描或测试**，这可能违法。

---

*最后更新：2026-09-11*
